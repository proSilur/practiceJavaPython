package codeabbey;

public class ModuloAndTimeDifference {

    //Solution by python 8-)

//    from datetime import datetime
//for i in range(int(input())):
//    a1, b1, c1, d1, a2, b2, c2, d2 = map(int, input().split())
//    a = datetime(2018, 1, a1, b1, c1, d1)
//    b = datetime(2018, 1, a2, b2, c2, d2)
//    delta1 = b - a
//    print("(" + str(delta1.days), int(delta1.seconds / 3600), int(delta1.total_seconds() / 60 % 60),
//    str(int(delta1.total_seconds() % 60)) + ")")

}
